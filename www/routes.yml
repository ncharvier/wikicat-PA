/:
  controller: General
  action: home

/login:
  controller: User
  action: login

/register:
  controller: User
  action: register

/accountUpdate:
  controller: User
  action: accountUpdate

/logout:
  controller: User
  action: logout

/valideAccount:
  controller: User
  action: valideAccount

/forgotPassword:
  controller: User
  action: forgotPassword

/acquireNewPassword:
  controller: User
  action: acquireNewPassword

/changePassword:
  controller: User
  action: changePassword

/page404:
  controller: Error
  action: page404

/page500:
  controller: Error
  action: page500

/admin/dashboard:
  controller: Admin
  action: dashboard

/admin/user:
  controller: Admin
  action: user

/admin/role:
  controller: Admin
  action: role

/admin/roleCreate:
  controller: admin
  action: createRole

/admin/roleUpdate:
  controller: admin
  action: updateRole

/admin/roleDelete:
  controller: admin
  action: deleteRole

/admin/pageList:
  controller: Admin
  action: pageList

/admin/pageTemplate:
  controller: Admin
  action: pageTemplate

/admin/comment:
  controller: Admin
  action: comment

/admin/visualSetting:
  controller: Admin
  action: visualSetting

/admin/exportTheme:
  controller: Admin
  action: exportTheme

/admin/plugin:
  controller: Admin
  action: plugin

/admin/globalSetting:
  controller: Admin
  action: globalSetting

/searchPage:
  controller: WikiPage
  action: searchPage

