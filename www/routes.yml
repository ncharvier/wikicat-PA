/:
  controller: general
  action: home

/login:
  controller: user
  action: login

/register:
  controller: user
  action: register

/accountUpdate:
  controller: user
  action: accountUpdate

/logout:
  controller: user
  action : logout

/page404:
  controller: error
  action: page404

/page500:
  controller: error
  action: page500

/admin/dashboard:
  controller: admin
  action: dashboard